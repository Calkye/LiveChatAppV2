const AddFriend = (io)=>{
  // io.on('connection', (socket)=>{
  //   socket.on('AddFriend', (data)=>{
  //     const username = data; 
  //     console.log(`Username: ${username}`); 
  //   }); 


  //   socket.on('disconnect', ()=>{
  //     console.log('Session ended with id of ', socket.id)
  //   })
  // })
}

export default AddFriend